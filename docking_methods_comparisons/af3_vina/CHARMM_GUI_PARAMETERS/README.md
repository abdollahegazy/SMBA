This directory is same sims as before but using FF parameters 
from PDB instead of the SMILES stuff from earlier.
This is because the force field atom ordering is differnt using SMILES
which caused significant unbinding in vina_boltz sims so doing same here

