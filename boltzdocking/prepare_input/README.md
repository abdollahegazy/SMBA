`process_boltz_inputs.py` creates the YAML files for boltz inputs by taking care of all the formatting. It yanks the MSA and ligand info stuff from an AF3 docking folder.

`create_boltz_scripts.py` creates `.sh` run scripts to execute each YAML.